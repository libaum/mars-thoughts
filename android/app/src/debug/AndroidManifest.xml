<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Debug only: lets the personal flavor talk plain http:// to a sync hub
         running on the laptop for LAN testing. Release builds keep Android's
         default (cleartext blocked) and the app itself refuses non-https URLs
         outside debug (SyncService.pairDevice).
         The "… Debug" label comes from the appLabel placeholder set per
         variant in build.gradle.kts, not from an override here. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
